spring:
  h2.console:
      enabled: true
      path: '/console'
  datasource:
    url: 'jdbc:h2:file:~/Downloads/todoApp/todoApp/todo-db'
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    baseline-on-migrate: true